"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.emitStructured = void 0;
const axios_1 = __importDefault(require("axios"));
const validation_1 = require("../../event/validation");
const defaults = {
    headers: {
        "content-type": "application/cloudevents+json; charset=utf-8"
    }
};
function emitStructured(event, options) {
    const config = Object.assign(Object.assign(Object.assign({}, defaults), options), { method: "POST", data: format(event) });
    return axios_1.default.request(config);
}
exports.emitStructured = emitStructured;
function format(event) {
    const formatted = JSON.parse(JSON.stringify(event));
    return JSON.stringify(formatted, (key, value) => {
        if (key === "data" && validation_1.isBinary(value)) {
            return validation_1.asBase64(value);
        }
        return value;
    });
}
