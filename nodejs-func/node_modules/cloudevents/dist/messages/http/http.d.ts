import { CloudEvent } from "../..";
import { Message, Sender } from "..";
export declare function send(sender: Sender, message: Message): Promise<boolean>;
export declare function binary(event: CloudEvent): Message;
